import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  joinLiveSession: {
    id: 'learn.custom.liveSessionBanner.join',
    defaultMessage: 'Join live session',
    description: 'Default link label for live session banner',
  },
});

export default messages;
