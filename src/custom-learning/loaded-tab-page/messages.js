import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  loadedTabPageWrapper: {
    id: 'learn.custom.loadedTabPage.wrapper',
    defaultMessage: 'Custom loaded tab page',
    description: 'Accessibility label for custom loaded tab page wrapper',
  },
});

export default messages;
